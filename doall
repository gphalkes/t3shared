#!/bin/bash

cd `dirname "$0"`/..
for i in makesys shared key window widget config highlight edit testenv ; do
	[ -d "$i" ] || continue
	echo "==== Executing $@ for $i ===="
	( cd "$i" ; "$@" )
done

