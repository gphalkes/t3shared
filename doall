#!/bin/bash

cd `dirname "$0"`/..
for i in makesys t3shared transcript t3config t3key t3highlight t3window t3widget tilde ; do
	[ -d "$i" ] || continue
	echo "==== Executing $@ for $i ===="
	( cd "$i" ; "$@" )
done

