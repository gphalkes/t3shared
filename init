#!/bin/bash

cd "$(dirname "$0")"
URL=$(git remote -v | grep '^origin ' | grep '\(fetch\)' | sed -r 's/^origin ([^ ]+) .*/\1/')

cd ..
for i in tilde key window widget config highlight makesys ; do
	[ -d "$i" ] && continue

	git clone "$URL/$i"
done
